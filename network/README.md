# Network

네트워크는 `multichat-java`와 가장 직접적으로 연결되는 CS 영역입니다.

먼저 아래 순서로 공부합니다.

1. `tcp-vs-udp.md`
2. `socket.md`
3. `tcp-message-framing.md`
4. `http.md`
5. `dns.md`
6. `rest-api.md`

면접에서 자주 연결되는 질문:

- TCP와 UDP의 차이는 무엇인가?
- TCP는 왜 신뢰성이 있다고 말하는가?
- HTTP는 TCP 위에서 어떻게 동작하는가?
- Socket은 무엇인가?
- 채팅 프로젝트에서 왜 TCP를 사용했는가?
- TCP 메시지 경계 문제는 어떻게 해결했는가?
